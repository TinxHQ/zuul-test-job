- job:
    name: wazo-js-sdk-yarn-test
    description: Build and test
    pre-run: zuul.d/pre.yaml
    run: zuul.d/run.yaml

- project:
    check:
      jobs:
        - wazo-js-sdk-yarn-test:
            nodeset: main
    gate:
      jobs:
        - wazo-js-sdk-yarn-test:
            nodeset: main
    auto-merge:
      jobs:
        - wazo-js-sdk-yarn-test:
            nodeset: main
