- job:
    name: wazo-js-sdk-yarn-test
    description: Build and test
    pre-run: zuul.d/pre.yaml
    run: zuul.d/run.yaml

- project:
    check:
      jobs:
        - wazo-js-sdk-yarn-test:
            nodeset: conteneur
    gate:
      jobs:
        - wazo-js-sdk-yarn-test:
            nodeset: conteneur
    auto-merge:
      jobs:
        - wazo-js-sdk-yarn-test:
            nodeset: conteneur
